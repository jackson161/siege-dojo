I can do this, but I need one thing first: I don’t have direct access to clone or read that GitHub repo from this chat, so I can’t reliably output **full-file, compile-ready contents** without seeing the current files.

Please paste these files (or upload a tarball/snippets), and I’ll return the exact full-file outputs in your required format:

1. `src/systems/actions.cairo`
2. `src/systems/commit_reveal.cairo`
3. `src/systems/resolution.cairo`
4. `src/models/*` (especially where existing Dojo models are declared, plus `mod.cairo`/`lib.cairo`)
5. test entry files (`src/tests/*`, `Scarb.toml`, and test `mod.cairo` if present)

If you want, I can also give you a **best-guess patch right now** based on standard Dojo patterns, but it may not compile against your exact codebase.